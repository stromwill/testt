name: RDP_PRO_FINAL_FIXED

on:
  workflow_dispatch:

jobs:
  rdp-admin-pro:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
      - name: 🚀 System Profiling & Optimization
        id: profile
        run: |
          echo "::group::Optimizing Windows"
          # RDP & Admin Config
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 1 -Force
          Set-ItemProperty -Path "HKCU:\Control Panel\Desktop" -Name "Wallpaper" -Value ""
          netsh advfirewall firewall set rule group="remote desktop" new enable=Yes
          
          # FIX ERROR RAM: Menggunakan Measure-Object untuk menjumlahkan semua stick RAM
          $cpu = (Get-CimInstance Win32_Processor).Name
          $totalRam = (Get-CimInstance Win32_PhysicalMemory | Measure-Object -Property Capacity -Sum).Sum
          $ramGB = [Math]::Round($totalRam / 1GB)
          $ram = "[$ramGB GB]"
          
          # Ambil Region (Gunakan Try-Catch agar tidak error jika internet delay)
          try { $region = (Invoke-RestMethod ipinfo.io).region } catch { $region = "Unknown" }
          
          echo "CPU=$cpu" >> $env:GITHUB_OUTPUT
          echo "RAM=$ram" >> $env:GITHUB_OUTPUT
          echo "REGION=$region" >> $env:GITHUB_OUTPUT
          echo "::endgroup::"

      - name: 🔑 Secure Credential Generation
        id: user_gen
        run: |
          $pass = -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 20 | ForEach-Object {[char]$_})
          echo "::add-mask::$pass"
          $secPass = ConvertTo-SecureString $pass -AsPlainText -Force
          New-LocalUser -Name "Pro_Admin" -Password $secPass -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "Pro_Admin"
          echo "PASS=$pass" >> $env:GITHUB_OUTPUT

      - name: 🌐 Tailscale Mesh Deployment
        id: ts
        run: |
          echo "::group::Installing Tailscale"
          Invoke-WebRequest -Uri "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi" -OutFile "ts.msi"
          Start-Process msiexec.exe -ArgumentList "/i", "ts.msi", "/quiet", "/norestart" -Wait
          
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=PRO-$(Get-Random)
          
          $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
          echo "IP=$tsIP" >> $env:GITHUB_OUTPUT
          echo "::endgroup::"

      - name: 📤 Professional Telegram Notification
        run: |
          $msg = @"
          💎 *PRO RDP SESSION ACTIVE*
          ━━━━━━━━━━━━━━━━━━━━
          🚀 *Host:* ``${{ steps.ts.outputs.IP }}``
          👤 *User:* ``Pro_Admin``
          🔑 *Pass:* ``${{ steps.user_gen.outputs.PASS }}``
          ━━━━━━━━━━━━━━━━━━━━
          💻 *CPU:* ${{ steps.profile.outputs.CPU }}
          🧠 *RAM:* ${{ steps.profile.outputs.RAM }}
          🌍 *Region:* ${{ steps.profile.outputs.REGION }}
          ━━━━━━━━━━━━━━━━━━━━
          ⚡ @TipsUNIX - Status: *ADMIN*
          "@
          
          $body = @{ chat_id="${{ secrets.TELEGRAM_CHAT_ID }}"; text=$msg; parse_mode="MarkdownV2" } | ConvertTo-Json
          Invoke-RestMethod -Uri "https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage" -Method Post -Body $body -ContentType "application/json"

      - name: ⏳ Keep-Alive & Monitoring
        run: |
          $start = Get-Date
          while ($true) {
            $diff = (Get-Date) - $start
            if ($diff.TotalMinutes -ge 350) { break }
            Start-Sleep -Seconds 60
          }

      - name: 🧹 Auto-Cleanup (Always Run)
        if: always()
        run: |
          $tsPath = "$env:ProgramFiles\Tailscale\tailscale.exe"
          if (Test-Path $tsPath) {
              Write-Host "Disconnecting Tailscale..."
              & $tsPath logout
          } else {
              Write-Host "Tailscale not installed. Cleanup skipped."
          }
